<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/root"
    android:layout_width="match_parent"
    android:layout_height="320dp"
    android:orientation="vertical"
    android:layout_margin="@dimen/default_size">

    <com.github.florent37.shapeofview.shapes.RoundRectView
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        app:shape_roundRect_topLeftRadius="@dimen/default_a_size"
        app:shape_roundRect_topRightRadius="@dimen/default_a_size"
        app:shape_roundRect_bottomLeftRadius="@dimen/default_a_size"
        app:shape_roundRect_bottomRightRadius="@dimen/default_a_size">

        <ImageView
            android:id="@+id/iv_poster"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:scaleType="center"
            android:background="@color/baseBgColor"
            android:contentDescription="@string/popular_poster" />

    </com.github.florent37.shapeofview.shapes.RoundRectView>

    <TextView
        android:id="@+id/tv_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        style="@style/titleCardText"
        android:lines="1"
        android:ellipsize="end"
        android:layout_marginTop="@dimen/default_size"
        tools:text="@string/app_name"/>
</LinearLayout>